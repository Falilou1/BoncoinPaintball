{% extends 'base.html.twig' %}

{% block title %}Messagerie
{% endblock %}

{% block body %}
	<section class="container">
		<div class="row">
			<div class="col-12">
				<h1>Messagerie</h1>
                <div>{{ message.title }}</div>
                <div>Message de {{ message.sender.email }}</div>
                <div>Le {{ message.createdAt|date }}</div>
                <div>{{ message.message }}</div>
				
               
			</div>
		</div>
	</section>
	
            </div>
		</div>
	</div>
{% endblock %}