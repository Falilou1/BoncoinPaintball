
<div class="card mb-4 box-shadow">
<div class="img-card-canvas">
    <img class="card-img-top img-fluid img-card"
        src="{% if advert.imageFile is defined %}
                {{ vich_uploader_asset(advert, 'imageFile') }}
            {% else %}
                {{ asset('build/images/paintball-2_1920.d71fc36e.jpg') }}
            {% endif %}" alt="Card image cap">
</div>
<div class="card-body">
    <h5 class="card-title advert-title">{{ advert.title }}</h5>
    <div class="card-text">
    <p>{{ advert.brand }} </p>
        <p>{{ advert.price }} €</p>
    </div>
    <p class="text-center">
        <a href="{{ path('adverts_show', {'id': advert.id}) }}" class="btn btn-primary">Voir</a>
    </p>
</div>
</div>